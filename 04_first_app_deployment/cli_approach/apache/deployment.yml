kind: Deployment
apiVersion: apps/v1
metadata:
  name: apache-deployment
spec:
  replicas: 2                 # total no. of pod to be created
  selector:                   # tell pod name whose relplica should be 2
    matchLabels: 
      app: apache
  template: 
    metadata:
      name: apache-deployment-pod   #name of pod
      labels:
        app: apache

    spec:
      containers:
      - name: apache
        image: httpd:latest
        ports:
        - containerPort: 80

   