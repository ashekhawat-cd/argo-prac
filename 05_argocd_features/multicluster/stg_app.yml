apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: apache-stg
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/ashekhawat-cd/argo-prac.git 
    targetRevision: main
    path: 04_first_app_deployment/cli_approach/apache      # Path to apache manifests
  destination:
    server: https://trainaks-training-1ac2ca-b61zelaq.hcp.canadacentral.azmk8s.io:443   # from `argocd cluster list`
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true